// Weektris configuration — fill these in once when you set up the repo.
// See README.md for exact step-by-step instructions.
window.WEEKTRIS_CONFIG = {
  // OAuth 2.0 Client ID from Google Cloud Console → APIs & Services → Credentials.
  // Must be a "Web application" client with this page's URL added under
  // "Authorized JavaScript origins", e.g. https://your-org.github.io
  // (no trailing slash, no path).
  GOOGLE_CLIENT_ID: "PASTE_YOUR_GOOGLE_CLIENT_ID_HERE.apps.googleusercontent.com",

  // The GitHub repo this page is deployed from — used to read/write data.json.
  GITHUB_OWNER: "your-github-org-or-username",
  GITHUB_REPO: "weektris",
  GITHUB_BRANCH: "main",
  DATA_PATH: "data.json"
};
